import HeaderText from "./HeaderText";
import Calculator from "../Calculator/Calculator";

const Header = () => {
  return (
    <header>
      <HeaderText />
      <Calculator />
    </header>
  );
};

export default Header;
