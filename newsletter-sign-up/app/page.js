import HeroImg from "@components/HeroImg";
import FormContainer from "@components/FormContainer";

const Home = () => {
  return (
    <main>
      <HeroImg />
      <FormContainer />
    </main>
  );
};

export default Home;
